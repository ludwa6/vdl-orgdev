# ARCHIVE — January 2026 OrgDev documentation

**Retired 2026-08-15. Do not use these as reference.** Kept for history only.

These sixteen documents were written 16–21 January 2026 and describe the **HolaFlow-era Notion
schema** — hence `HOLAFLOW-DATABASE-RECONCILIATION.md`. They were superseded twice:

| Generation | Where it lives now | Status |
|---|---|---|
| 1 — January (these files) | `archive/2026-01-orgdev-docs/` | retired |
| 2 — February | `docs/` in this repo | current, with corrections applied |
| 3 — August corrections | folded into `docs/data-entry-sop.md` | current |

Two further cautions:

- **Several are stubs.** `COMPLETE-DATABASE-FIELDS-SPEC.md` and
  `HOLAFLOW-DATABASE-RECONCILIATION.md` open with *"See downloaded file for complete
  specification."* The referenced downloads were never committed anywhere.
- **`ORGDEV-CURRENT-SCHEMA-2026-01-21.md` is not current** despite its name. The live schema is
  generated to `docs/generated/schema.md`.

`scripts/` holds two January utilities. `convert_md_to_docx.py` may be worth reviving for the
handbook generator; `notion_to_kumu_sync.py` targets Kumu, which is not in the current architecture.
